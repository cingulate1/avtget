# avtget

Personal media download/transcription desktop app.

## First Run

`config.ini` is intentionally not committed. On first launch, the app creates/prompts for the required local paths and settings.

Ignored local/runtime files include:

- `config.ini`
- `avtget_temp/`
- `node_modules/`
- Rust/Tauri `target/` directories